# NOTE: this pins the upstream SD3/SDXL stack for traning/train_sd3_pix2pix.py and
# traning/train_sdxl_pix2pix.py (with `cd diffusers && pip install -e .`).
# The FLUX.2 klein scripts in flux/ need a DIFFERENT, incompatible stack -- see
# flux/requirements.txt and use a separate venv for them.
accelerate==0.31.0
clip @ git+https://github.com/openai/CLIP.git@dcba3cb2e2827b402d2701e7e1c7d9fed8a20ef1
datasets==2.20.0
huggingface-hub==0.23.4
numpy==1.26.3
omegaconf==2.3.0
openai-clip==1.0.1
sentencepiece==0.2.0
tokenizers==0.19.1
torch==2.3.0+cu118
transformers==4.41.2
xformers